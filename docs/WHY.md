# Why This Project Exists

Most conversational AI assumes the user can look at a screen, type, or speak freely. This project explores a different interaction model: the AI adapts its conversation to a tiny set of silent physical inputs from AirPods and only falls back to speech when the user deliberately asks for freeform input.
